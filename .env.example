# tmp mail API base URL.
MAIL_API_BASE=

# Vercel KV (Upstash Redis) — required for distributed rate limiting on Vercel.
# These are auto-added when you link a KV database in the Vercel dashboard.
KV_REST_API_READ_ONLY_TOKEN=
KV_REST_API_TOKEN=
KV_REST_API_URL=
KV_URL=
REDIS_URL=
